{
  "framework": "nextjs",
  "installCommand": "npx pnpm@9.15.0 install --no-frozen-lockfile",
  "buildCommand": "npx pnpm@9.15.0 exec next build --webpack",
  "outputDirectory": ".next",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
