# Next
/.next/
/out/
next-env.d.ts

# Capacitor (Android se genera con `pnpm cap:add`)
/android/

# Tauri (Windows) — icons/ SÍ se commitean (CI los necesita para el .ico de Windows)
/src-tauri/target/
/src-tauri/gen/

# deps / env
/node_modules/
.env*.local
